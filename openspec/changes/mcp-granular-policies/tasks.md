# Tasks: Policies MCP granulares (RUN-1597)

Specs: `mcp-policy-scope`, `mcp-policy-plan-selection`, `mcp-tool-allowlist`, `mcp-policy-scope-telemetry`. Diseño: `design.md` (autoritativo). Rama de integración: `develop` (protegida; nunca commitear ahí). PR único en un worktree hermano, rama `feat/run-1597-mcp-policy-scope`, con RUN-1597 y `size:exception` en el cuerpo; cada slice es un commit de unidad de trabajo.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ≈3.000 a mano + ≈430 generadas (mock mockery, `docs/swagger.*`, `docs/openapi.json`) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | **PR único** `feat/run-1597-mcp-policy-scope` → `develop`, con 10 commits de unidad de trabajo en el orden 1a → 1b → 2a → 1c → 1d → 2b → 3a → 3b → 3c → 3d (ver tabla) |
| Delivery strategy | exception-ok |
| Chain strategy | size-exception |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: size-exception
400-line budget risk: High

**Decisión (Edu, 2026-09-16):** un solo PR con `size:exception` aceptada por el maintainer. Los 10 slices se conservan como **commits de unidad de trabajo** (skill `work-unit-commits`) dentro de la misma rama, cada uno verde en su gate y reviewable por separado en la pestaña de commits. Orden de ejecución **1a → 1b → 2a → 1c → 1d → 2b → 3a → 3b → 3c → 3d**: 2a se adelanta a la Admin API para que una policy con `mcp_scope` creada antes del reorden del dispatcher quede dormida (fuera del plan base) en vez de correr consumer-wide. El cuerpo del PR cita RUN-1597, esta tabla y la etiqueta `size:exception`.

### Medición de superficie (base de la estimación)

| Zona | Medido |
|---|---|
| `CallTool` en tests (mock `Composer`/dispatcher) | 23 sitios en 7 ficheros: `rpc_dispatcher_test.go` 10, `rpc_dispatcher_span_test.go` 3, `mcp_handler_test.go` 3, `mcp_handler_unreachable_test.go` 2, `middleware/mcp_pipeline_integration_test.go` 1, `mcp_audit_regression_test.go` 3, `naming_test.go` 1; más 7 llamadas directas en `composer_test.go` (6 `TestComposer_CallTool_*`). `mocks/mcp_composer_mock.go` 516 líneas (11 menciones). El diseño decía 17/7. |
| Admin API policy | `request/*.go` 192, `response/policy_response.go` 71, handlers create/update/list 286, `consumer/association_handler.go` 211; sin `_test.go` en el paquete (cobertura en `tests/functional/{create,list}_policy_test.go`, `consumer_associations_test.go`: 582) |
| Persistencia | `repository/policy/repository.go` 410; patrón `consumer/registry_prune.go` 166; `tests/functional/repositories/policy/repository_test.go` 494 |
| DataFinder / plan | `data_finder.go` 326 (+test 387), `consumer_data.go` 115, `plugins/plan.go` 187, `chain.go` 91 (+`plan_test.go` 162) |
| Hot path MCP | `composer.go` 378, `rpc_dispatcher.go` 353, `plugin_runner.go` 369 (+test 400) |
| `toolallowlist` | `plugin.go` 411, `config.go` 87 (+`plugin_test.go` 664) |
| Telemetría | `trace/span.go` 372, `events/event.go` 187, `metrics/builder.go` 545 (+test 839) |
| Generados | `docs/openapi.json` 9.447, `swagger.json` 8.394, `swagger.yaml` 5.784; `make openapi` = `swag init` + `swagger2openapi` |
| Funcional | `mcp_plugin_chain_test.go` 179 (un upstream, 4 tests) |

Con tests, los tres PRs del diseño miden ≈630 / ≈630 / ≈1.400 y la partición 1a/1b/2/3a/3b sugerida sigue >400 en cada pieza (≈630 / ≈600+gen / ≈630 / ≈760 / ≈630). Se entrega como **un PR con `size:exception`** y 10 commits de unidad de trabajo; el forecast por slice sigue siendo la guía para revisar commit a commit.

### Suggested Work Units

Una rama `feat/run-1597-mcp-policy-scope` (worktree hermano) sobre `develop`. Cada slice es **un commit** (o un grupo pequeño) con su gate verde antes del siguiente. Líneas = a mano / generadas.

| Orden | Slice | Commit | Objetivo | Líneas | Verificación | Sin efecto en runtime porque… |
|---|---|---|---|---|---|---|
| 1 | 1a `domain` | `feat(policy): add MCPScope domain type` | `MCPScope`, `Matches`, `Validate`, `PruneRegistry`, campo en `Policy` | ≈345 / 0 | `go test ./pkg/domain/policy/...` | nadie lee el campo; `Rehydrate` con `nil` |
| 2 | 1b `persistence` | `feat(policy): persist mcp_scope and prune on registry delete` | migración, columna, filtro `registry_id`, prune en tx, hook, snapshot round-trip | ≈290 / 0 | `make test-repositories`, `go test ./pkg/infra/configsnapshot/...` | columna `NULL`; el proto no cambia; la API aún no la expone |
| 3 | 2a `plan-ordering` | `feat(plugins): partition scoped policies and add specificity tie-break` | `specificity`, comparador, `Union`, `InstanceOf`, `partitionScoped`, anulación `slug` acotada | ≈180 / 0 | `go test ./pkg/app/plugins/... ./pkg/app/consumer/... ./pkg/app/store/...` | plan base = sin scope; una policy con scope queda **dormida** hasta 3b (nunca consumer-wide) |
| 4 | 1c `app-validation` | `feat(policy): validate mcp_scope on create/update and attach` | `CreateInput`/`MCPScopePatch`, `validateMCPScope`, associator LLM | ≈270 / 0 | `go test ./pkg/app/policy/... ./pkg/app/consumer/...` | inputs sin DTO que los rellene |
| 5 | 1d `admin-api` | `feat(api): expose mcp_scope, warnings and registry_id filter` | DTOs, `warnings`, `?registry_id`, funcionales, `make openapi` + contract test | ≈335 / ≈250 | `make test-functional`, `go test ./docs/...` | API viva; con 2a ya dentro, una policy con scope no se ejecuta hasta 3b |
| 6 | 2b `policy-plans` | `feat(consumer): precompile PolicyPlans per registry and tool` | `PolicyPlans`, `BuildPolicyPlans`, `PlanFor`, `MCPPlans` en consumers y Store | ≈345 / 0 | `go test ./pkg/app/consumer/...`, `make test-race` | `MCPPlans` construido y sin lector |
| 7 | 3a `composer-resolve` | `refactor(mcp): split Composer.CallTool into Resolve and Invoke` | `Resolve`/`Invoke` junto a `CallTool` (wrapper), mock, `ToolCall`, metadata `mcp.*` | ≈255 / ≈120 | `go test ./pkg/app/mcp/... ./pkg/api/...` | `CallTool` = `Resolve`+`Invoke`; dispatcher igual |
| 8 | 3b `dispatcher-reorder` | `feat(mcp): resolve the tool before running scoped policies` | reorden de `callTool`, borrar `CallTool`, migrar 30 sitios de test | ≈345 / ≈60 | ídem + `make test-race`, `make test-functional` | **primer commit con efecto**; revert = plan consumer-wide |
| 9 | 3c `tool-allowlist` | `feat(plugins): tool_allowlist on MCP as deny primitive` | `ProtocolMCP`, funcionales dos upstreams, benchmark | ≈380 / 0 | `go test -tags functional -race -run MCPPluginChain ./tests/functional/`, bench | plugin opt-in por asociación |
| 10 | 3d `telemetry` | `feat(mcp): record scope decisions on the tools/call span` | `Explain`/`ScopeDecision`, span, evento, docs | ≈250 / 0 | `go test ./pkg/app/... ./pkg/infra/trace/...` | solo con span activo |

Total previsto ≈3.000 líneas a mano + ≈430 generadas en un único PR. La revisión se hace commit a commit siguiendo esta tabla; el cuerpo del PR enlaza `tasks.md` y justifica la `size:exception` (feature indivisible en runtime hasta 3b, generados de mockery y OpenAPI).

## Fase 1 — Commit 1 · slice 1a `domain`

- [x] 1.1 Crear `pkg/domain/policy/mcp_scope.go` con `MCPToolRef`, `MCPScope`, `MCPTarget`, `MCPCaller`, `SkipReason` (`SkipDestination|SkipPrincipal|SkipExcept`) tal cual en design §Interfaces. [mcp-policy-scope]
- [x] 1.2 `IsEmpty`, `HasDestination`, `HasPrincipal`, `Specificity` (tools=2 > registry=1 > 0; ×2 + bit principal). Test `TestMCPScope_Specificity` en `mcp_scope_test.go`. [mcp-policy-plan-selection §Precedencia]
- [x] 1.3 `MatchesTarget(MCPTarget)`: `registry ∈ RegistryIDs` o `(registry, tool) ∈ Tools`; sin destino → `true`. Test con tool nativa (el nombre expuesto no entra). [mcp-policy-scope §Tools por (registry_id, nombre nativo)]
- [x] 1.4 `MatchesCaller(MCPCaller) (bool, SkipReason)`: `Subject` o `Email` ∈ `Users`; `Groups` con igualdad exacta tras `TrimSpace` (regla `Grant.Allows`); caller sin identidad no matchea `users`/`groups` ni cae en `except_*`. Tests: email con mayúsculas, `sub` sin email, API key, "todos menos Finanzas" con y sin identidad. [§Identificación del principal, §Excepciones]
- [x] 1.5 `Matches(t, c) (bool, SkipReason)`: `nil` → `true`; `{}` → `false`; destino AND principal, luego except. `TestMCPScope_Matches` table-driven: nil, `{}`, DLP Finanzas en Snowflake, Finanzas en jira, Marketing en snowflake. [§nil frente a scope vacío, §Destino AND principal]
- [x] 1.6 `Validate()` → `ErrInvalidMCPScope` (nuevo en `errors.go`): duplicados, ids nil, `tool`/`users`/`groups` vacíos, registry en `registry_ids` y `tools`; NO exige entradas (una policy podada a `{}` debe renombrarse). `Normalize()`: `TrimSpace`, emails a minúsculas. Tests por regla. [§Validación en la Admin API]
- [x] 1.7 `(*Policy).PruneRegistry(id) bool`: quita `id` de `RegistryIDs` y `Tools`; si queda vacío deja `&MCPScope{}`. Tests "Varios registries" y "Único registry" (`{}` y `Matches` → `false`). [§Prune al borrar un registry]
- [x] 1.8 `policy.go`: `MCPScope *MCPScope json:"mcp_scope,omitempty"` en `NewPolicy`/`Rehydrate`; `Validate()` delega. `repository.go`: `ListFilter.RegistryID *ids.RegistryID`. Ajustar `policy_test.go`.
- [x] 1.9 Gate: `go test ./pkg/domain/policy/...`, `make lint`, `clean-comments` sobre los `.go` tocados.

## Fase 2 — Commit 2 · slice 1b `persistence`

- [x] 2.1 Migración `pkg/infra/database/migrations/<YYYYMMDD>120000_add_policy_mcp_scope.go` (timestamp del día del merge; cierra la Open Question): `ALTER TABLE policies ADD COLUMN IF NOT EXISTS mcp_scope JSONB NULL`; `Down`: `DROP COLUMN IF EXISTS`. Patrón `20260909120000_drop_roles_and_routing_mode.go`.
- [x] 2.2 `pkg/infra/repository/policy/repository.go`: columna en `Save`, `Update`, selects de `FindByID`/`FindByIDs`/`ListByGateway`/`List`, `scanPolicy` (`NULL` → nil; `'{}'` → `&MCPScope{}`); filtro `ListFilter.RegistryID` con `?` sobre `registry_ids` y `@>` sobre `tools` (SQL del design).
- [x] 2.3 Crear `pkg/infra/repository/policy/registry_prune.go`: `(*Repository).PruneRegistryReferencesTx(ctx, tx pgx.Tx, gatewayID, registryID) (registrydomain.PruneReport, error)` (`SELECT … FOR UPDATE`, `PruneRegistry`, `UPDATE` escribiendo `{}`); misma firma que `registryrepo.DeleteHook`; patrón `consumer/registry_prune.go`. [§Prune "nunca NULL"]
- [x] 2.4 `pkg/domain/registry/prune_report.go`: `PolicyPrune{PolicyID, Emptied}`, `PruneReport.Policies`; `Merge` y `Empty` lo contemplan.
- [x] 2.5 `pkg/container/modules/registry.go`: segundo `registryrepo.WithDeleteHook(deps.Policies.PruneRegistryReferencesTx)`; `modules/policy.go` expone el `*policyrepo.Repository` concreto. Verificar que `pkg/runtimeconfig/snapshot/adapters/policy_repository.go` no cambia (prune fuera del puerto → data plane no-op). [§Prune "data plane no-op"]
- [x] 2.6 `pkg/infra/configsnapshot/codec_test.go`: round-trip de `mcp_scope` nil / `{}` / completo.
- [x] 2.7 `tests/functional/repositories/policy/repository_test.go`: insert/update/scan de los tres estados; `List` con `RegistryID` (en `registry_ids` y en `tools`). `repositories/registry/repository_test.go`: borrar registry poda en la misma tx y persiste `{}`.
- [x] 2.8 Gate: `make test-repositories` (`PG_TEST_URL`), `go test ./pkg/infra/configsnapshot/...`, `clean-comments`. — Evidencia (sdd-verify, 2026-09-17): repositorios contra Postgres local (`PG_TEST_URL=postgres://postgres:postgres@localhost:5432/trustgate_repo_test`), 6 paquetes `ok` con `TestRepository_MCPScope_RoundTrip`, `_MCPScope_UpdateTransitions`, `_List_FilterByRegistryID`, `_DeleteRegistry_PrunesMCPScopeInSameTx` PASS (`/tmp/claude-501/repo-tests.log`); `TestCodecRoundTripsPolicyMCPScope` PASS en `make test-race` (`/tmp/claude-501/test-race.log`).

## Fase 3 — Commit 4 · slice 1c `app-validation` (tras 2a)

- [x] 3.1 `pkg/app/policy/validate.go`: `validateMCPScope(ctx, registryRepo, pluginReg, gatewayID, slug, scope)`: `FindByIDs` del mismo gateway, `IsMCP()`, plugin con `ProtocolMCP`, ≥1 entrada, `Normalize` + `Validate`. Nuevo `validate_test.go` con mock de `registrydomain.Repository`: otro gateway, registry LLM, plugin sin MCP, `{}`. [mcp-policy-scope §Validación en la Admin API]
- [x] 3.2 `creator.go`: `CreateInput.MCPScope *domain.MCPScope` → 3.1. `creator_test.go`: válido y rechazado.
- [x] 3.3 `updater.go`: `MCPScopePatch{Set bool; Value *MCPScope}` (omitido conserva; `Set && nil` borra; objeto valida y reemplaza); renombrar una policy con `{}` sigue pasando. `updater_test.go` tri-estado. [§Update omitido frente a null]
- [x] 3.4 `pkg/app/consumer/associator.go` `validatePolicyProtocol`: `MCPScope != nil` con consumer `TypeLLM` → `ErrPolicyProtocolMismatch`. `associator_test.go`. [§global con scope; consumers LLM] — Aplicado: guard `validatePolicyScope` separado (antes de `validatePolicyProtocol`, que no cambia) que rechaza `MCPScope != nil` (incluido `{}`) en cualquier consumer `Type != TypeMCP` con el nuevo `ErrPolicyScopeRequiresMCP`, que envuelve `ErrPolicyProtocolMismatch` (y por tanto `ErrValidation`).
- [x] 3.5 `pkg/container/modules/policy.go`: inyectar `registrydomain.Repository` y `appplugins.Registry` en creator/updater.
- [x] 3.6 Gate: `go test ./pkg/app/policy/... ./pkg/app/consumer/...`, `clean-comments`.

## Fase 4 — Commit 5 · slice 1d `admin-api`

- [x] 4.1 `request/create_policy_request.go`: `MCPScopeRequest`/`MCPToolRefRequest` + `toDomain`; `update_policy_request.go`: `MCPScope json.RawMessage` → `MCPScopePatch` (ausente / `null` / objeto); `list_policy_request.go`: `registry_id`. — Aplicado: los DTOs compartidos viven en `request/mcp_scope_request.go` (`MCPScopeRequest.ToDomain`, `parseMCPScopePatch`); `CreatePolicyRequest.ToMCPScope()`, `UpdatePolicyRequest.ToMCPScope() (set, scope, err)`, `ListPolicyRequest.RegistryID *ids.RegistryID`. El `request` no importa `app`: el handler construye `apppolicy.MCPScopePatch`. Un `registry_id` que no es uuid → `ErrValidation` (422 `validation_failed`, el mapeo del repo para validación; el `?registry_id=` inválido del listado → 400 `invalid_query`). Test unitario `mcp_scope_request_test.go`.
- [x] 4.2 `response/policy_response.go`: `mcp_scope` (ausente si nil, `{}` si podado) y `warnings []string,omitempty`; anotaciones swag en `create/update/list_policy_handler.go` (`@Param registry_id query`). — Aplicado: `MCPScopeResponse`/`MCPToolRefResponse`, `FromPolicyWithWarnings`; get/list/duplicate siguen con `FromPolicy` (sin `warnings`).
- [x] 4.3 Crear `pkg/app/policy/warnings.go`: `Warner.Overlaps(ctx, p) ([]string, error)` → `"consumer <id> already runs plugin <slug> without scope"`; `warnings_test.go`; create/update lo devuelven. [G3; §Filtro y aviso] — Aplicado: `Warner{Overlaps, OverlapsOnAttach}` sobre `policy.Repository` + `consumer.Reader`, sin efectos secundarios; alcance = `ConsumerIDs` o, si `Global`, todos los consumers `TypeMCP` del gateway; solo cuentan policies sin scope **habilitadas** (una global sin scope avisa por cada consumer alcanzado). Los handlers lo llaman tras la escritura (las firmas de `Creator`/`Updater`/`Associator` no cambian → sin regenerar sus mocks); si el Warner falla, se registra con `slog` y la respuesta sale sin `warnings` (la escritura ya es un hecho). `POST .../global` (`SetGlobal`) también devuelve `warnings`: el create no acepta `global`, así que es la única vía para el escenario "trustguard global con scope" del spec. Mock `mocks/policy_warner_mock.go` (mockery v2.53.7). Wiring en `pkg/container/modules/policy.go`.
- [x] 4.4 `consumer/association_handler.go`: attach → `204`, o `200 {"warnings":[…]}` solo con avisos. Confirmar con `app` antes de fijarlo (Open Question del design). — **Decisión aplicada (pendiente de confirmar con la UI de `app`)**: `204` sin cuerpo cuando no hay avisos (contrato actual intacto, `AttachPolicy` del helper funcional sigue exigiendo 204) y `200 {"warnings": [...]}` (`response.AttachPolicyResponse`) únicamente cuando hay al menos un aviso. Si `app` prefiere `200` siempre, el cambio es de una línea en `AttachPolicy` + helper funcional.
- [x] 4.5 Funcionales: `create_policy_test.go` (otro gateway 4xx, registry en ambas listas 4xx, `{}` 4xx, `global` con scope 2xx, `warnings` con doble `trustguard`), `list_policy_test.go` (`?registry_id=`), `consumer_associations_test.go` (scope en consumer LLM 4xx). [§Validación, §Global con scope] — Aplicado: `TestCreatePolicy_WithMCPScope_EchoesStoredScope`, `_WithoutMCPScope_OmitsField`, `_MCPScopeRejections` (9 casos 422), `_MCPScopeNotAnObjectRejected`, `TestPolicyGlobalWithMCPScope_WarnsAboutUnscopedTrustGuard` (201 → `/global` 200 con `warnings` → `?registry_id=`), `TestListPolicies_FilterByRegistryID` (en `registry_ids`, en `tools`, desconocido → vacío, no uuid → 400), `TestUpdatePolicy_MCPScopeTriState` (omitido conserva / objeto reemplaza / `{}` 422 / `null` borra), `TestAttachPolicy_ScopedPolicyOnLLMConsumerRejected` (422), `TestAttachPolicy_ScopedPolicyWarnsOnUnscopedOverlap` (204 limpio, 200 con `warnings`). Helpers `scopedPolicyPayload`, `createMCPRegistry` en `create_policy_test.go`. Compilan con `go vet -tags functional`; requieren Postgres (no ejecutados en local).
- [x] 4.6 `make openapi` (regenera `docs/swagger.{json,yaml}`, `docs/openapi.json`, `docs/docs.go`). `docs/openapi_test.go`: `TestPolicyOpenAPIDocumentsMCPScopeAndWarnings` (patrón `TestRegistryListOpenAPIDocumentsFlatAndGroupedShapes`): `mcp_scope` en `CreatePolicyRequest`/`UpdatePolicyRequest`/`PolicyResponse`, `warnings`, `registry_id` en el GET. — Aplicado: swag v1.16.4 + swagger2openapi 7.0.8; el diff de `docs/*` solo contiene esta slice. `UpdatePolicyRequest.mcp_scope` se documenta como `object` (`swaggertype`, es `json.RawMessage`) con la descripción del tri-estado; el test también cubre `200`/`204` del attach.
- [x] 4.7 Gate: `make test-functional`, `go test ./docs/...`, `clean-comments`. Cuerpo del PR: ficheros generados marcados `size:exception` (solo esa parte). — Hecho en local: `gofmt -l` vacío, `go vet ./...`, `go build ./...`, `go test -race ./pkg/api/... ./pkg/app/policy/... ./pkg/app/consumer/... ./pkg/container/... ./docs/...` ok, `go vet -tags functional ./tests/functional/...` ok, `make lint` 0 issues, barrido de comentarios. — Evidencia (sdd-verify, 2026-09-17): funcional completa `go test -tags functional -race -count=1 -p 1 ./tests/functional/...` contra Postgres local → `exit=0`, `tests/functional 32.318s` + 6 `repositories/*` `ok` (`/tmp/claude-501/func-full.log`); subconjunto policy/MCP `ok 18.216s` (`/tmp/claude-501/func-mcp-policy.log`) con 1 SKIP (`mcp_policy_scope_test.go:255`, exención positiva de `except_groups`); `go test ./docs/...` ok.

## Fase 5 — Commit 3 · slice 2a `plan-ordering` (se ejecuta antes que la Fase 3)

- [x] 5.1 `pkg/app/plugins/chain.go`: `chainEntry.specificity uint8` desde `pol.MCPScope.Specificity()` (nil → 0); lo rellenan `buildStageChain` y `NewStagePlan`.
- [x] 5.2 `plan.go` `groupBatches`: `priority asc → specificity desc → slug → id`; los batches paralelos siguen por `priority`. `plan_test.go`: "Empate de prioridad" y "Batch paralelo". [mcp-policy-plan-selection §Precedencia]
- [x] 5.3 `plan.go`: `(*StagePlan).Union(extra ...*StagePlan) *StagePlan` (concatena `byStage`, reagrupa, sin volver al `Registry`). Test de reagrupado.
- [x] 5.4 `pkg/domain/registry/registry.go`: `InstanceOf ids.RegistryID json:"instance_of,omitempty"`, `ScopeKey()`; `pkg/app/store/scoper.go` `instanceRegistry` lo rellena con el `ID` del shelf, `configuredRegistry` no cambia. Test en `scoper_test.go`. [§Store multi-instancia] — Aplicado (decisión del maintainer en 2a): `InstanceOf` lleva `json:"-"` (vista por request, nunca en snapshot ni en la API) y `configuredRegistry` también lo rellena con el `ID` del shelf (`ScopeKey()` devuelve el shelf en ambos clones; el shelf expuesto tal cual sigue con `InstanceOf` nil).
- [x] 5.5 `pkg/app/consumer/data_finder.go`: `partitionScoped(policies) (unscoped, scoped)`; `composePolicies` solo con sin scope; `buildPolicyPlan(unscoped)`; `StoreConsumer.Policies = globalsUnscoped`. `data_finder_test.go`: "Anulación clásica", "TrustGuard global con scope" (ambas sobreviven), "Discovery ignora el scope". [§Plan base sin scope, §Anulación por slug acotada]
- [x] 5.6 Gate: `go test ./pkg/app/plugins/... ./pkg/app/consumer/... ./pkg/app/store/...`, `clean-comments`.

## Fase 6 — Commit 6 · slice 2b `policy-plans`

- [x] 6.1 Crear `pkg/app/consumer/policy_plans.go`: `PolicyRef`, `scopedEntry`, `destPlans`, `PolicyPlans{base, anyDest, byRegistry, byTool, scoped}`, `BuildPolicyPlans(reg, unscoped, scoped, logger)` precompilando `static` por registry y `(registry, tool)` y listas `principal` por destino. — Aplicado: `byTool` indexa por `policydomain.MCPTarget` (conversión directa desde `MCPToolRef`); `byRegistry[R].static` = `NewStagePlan(unscoped ∪ static{R})`, `byTool[(R,T)].static` = `NewStagePlan(unscoped ∪ static{R} ∪ static{(R,T)})`; un destino sin estáticas propias comparte el puntero del plan padre (registry o `base`). Se descartan al construir las policies deshabilitadas, los scopes `{}` y las de plugin desconocido (plan vacío). `scoped` se rellena para `Explain` (3d).
- [x] 6.2 `PlanFor(reg, nativeTool, principal) *StagePlan`: nil receiver → nil; clave `reg.ScopeKey()`; sin principal-scoped → 2 lookups y 0 asignaciones; `MCPCaller` una vez por request (`Subject`, `Email()` en minúsculas, `Groups()`); con match → `Union`. — Aplicado: el orden interno lo decide `NewStagePlan`/`Union` (priority → specificity → slug → id), sin re-sort; `reg == nil` se trata como destino sin clave (cae a `base` + `anyDest`).
- [x] 6.3 `policy_plans_test.go`: sin scoped → mismo puntero `base`; `byTool` > `byRegistry`; clon con `InstanceOf`; "Solo destino" no llama a `Groups()`/`Email()` y `testing.AllocsPerRun == 0`; "Grupo sobre el registry"; "Dos policies coinciden" (unión). [§Plan base sin scope, §Filtro por principal opt-in, §Store multi-instancia; mcp-policy-scope §Destino AND principal] — Aplicado: 8 tests en `policy_plans_test.go` con un `recordingPlugin` propio (registrado bajo varios slugs, protocolo MCP) que ejecuta el plan vía `appplugins.NewExecutor(...).RunStage` y devuelve los nombres de policy en orden de ejecución; cubre nil receiver, base sin scoped (mismo puntero), tool > registry > base (más `{}` podado y policy deshabilitada fuera), clon `InstanceOf`, principal (grupo, email en mayúsculas, `sub` sin email, nil principal, sin match → mismo puntero estático), `except_*` con caller sin identidad, unión estática + principal, y `testing.AllocsPerRun(1000) == 0` en hit por tool, hit por registry y fallback a base con un principal que lleva `groups` (probaría que `Groups()` no se lee).
- [x] 6.4 `consumer_data.go`: `RoutableConsumer.MCPPlans *PolicyPlans`; `data_finder.go` lo construye por consumer y para `StoreConsumer` (`globalsUnscoped`, `globalsScoped`). `data_finder_test.go`: "Store" con global `registry_ids: [snowflake]`. [§Anulación por slug acotada; Store] — Aplicado: `buildMCPPlans` devuelve nil para consumers `Type != TypeMCP` (LLM/A2A) y delega en `BuildPolicyPlans`, que devuelve nil sin `pluginRegistry` (mismo fallback que `buildPolicyPlan`); el Store se construye con `(globalsUnscoped, globalsScoped)`. Tests `BuildsMCPPlansOnlyForMCPConsumers` y `StoreConsumerPlansFromScopedGlobals`.
- [x] 6.5 Gate: `go test ./pkg/app/consumer/...`, `make test-race`, `clean-comments`. — Hecho en local: `gofmt -l pkg/app/consumer` vacío, `go vet ./...`, `go build ./...`, `go test -race -count=1 ./pkg/app/consumer/... ./pkg/app/plugins/... ./pkg/app/mcp/... ./pkg/app/proxy/... ./pkg/api/handler/http/mcp/...` ok, `make lint` 0 issues, barrido de comentarios (solo doc comments exportados).

## Fase 7 — Commit 7 · slice 3a `composer-resolve`

- [x] 7.1 `pkg/app/mcp/composer.go`: `ResolvedTool{Registry, Tool, Exposed}`; `Resolve(ctx, rc, name)` (un `compose()`; precedencia actual `ToolNotPermittedError` > `ConsentRequiredError` > `ErrToolNotFound`) e `Invoke(ctx, rc, target, arguments)` (`annotateUpstream` + `invokeUpstream`). `CallTool` pasa a `Resolve`+`Invoke` hasta 3b. Ambos en el puerto `Composer`.
- [x] 7.2 `make generate` → `mocks/mcp_composer_mock.go` gana `Resolve`/`Invoke` (generado; `--with-expecter`).
- [x] 7.3 `composer_test.go`: `TestComposer_Resolve_{RoutesToOwningUpstream,UnknownToolStaysNotFound,DeniedBeatsPendingConsent,UnknownToolSurfacesPendingConsent}` y `TestComposer_Invoke_UsesResolvedTarget`. [§Errores de resolución antes de plugins]
- [x] 7.4 `pkg/infra/context/request_context.go`: `MetadataMCPTool = "mcp.tool"`, `MetadataMCPRegistryID = "mcp.registry_id"`, `MetadataMCPRegistryName = "mcp.registry_name"` (lo exige la spec §RequestContext en MCP), `MetadataMCPExposedTool = "mcp.exposed_tool"`.
- [x] 7.5 `plugin_runner.go`: `ToolCall{Exposed, Registry, NativeTool, Arguments, Plan}`; `PreRequest(ctx, rc, call)` / `PreResponse(ctx, rc, call, result)`; `buildRequestContext` rellena `RegistryID`, `Body{name: exposed, arguments}`, `Metadata mcp.*`; `Plan == nil` → `rc.PolicyPlan` (así `mcpRoutableConsumer()` sin `MCPPlans` sigue cayendo a `in.Policies`). `rpc_dispatcher.go` construye `ToolCall{Exposed: name, NativeTool: name}` sin `Resolve` aún. `plugin_runner_test.go`: metadata nativa, `Body.name` expuesto. [§RequestContext en MCP "Consumer federado"] **Aplicado en 3a:** `ToolCall` y `PreRequestCall(ctx, rc, call)` / `PreResponseCall(ctx, rc, call, result)` nuevos; `PreRequest`/`PreResponse` actuales delegan con `Registry=nil, Plan=nil` y `rpc_dispatcher.go` queda intacto (decisión del maintainer: el dispatcher cambia en 3b, que renombra `*Call` a las firmas del diseño y borra las antiguas).
- [x] 7.6 Gate: `go test ./pkg/app/mcp/... ./pkg/api/...`, `clean-comments`.

## Fase 8 — Commit 8 · slice 3b `dispatcher-reorder`

- [x] 8.1 `rpc_dispatcher.go` `callTool`: rate limit → meta-tools → `composer.Resolve` → `plan := rc.MCPPlans.PlanFor(target.Registry, target.Tool.Name, principal)` → `PreRequest(ToolCall)` → `Invoke(target, arguments)` → `PreResponse` con el mismo plan. `pre.Arguments` se respeta; `name` reescrito por un plugin → warning y se ignora. Fail-open RUN-832 intacto. [§Orden de callTool; §RequestContext "Plugin reescribe name", "Error no bloqueante"]
- [x] 8.2 Borrar `CallTool` de `Composer` y `composer.go`; `make generate` (el mock lo pierde).
- [x] 8.3 Migrar los 30 sitios medidos: `rpc_dispatcher_test.go` (10 → `EXPECT().Resolve`, `EXPECT().Invoke`, `AssertNotCalled(t, "Invoke", …)`), `rpc_dispatcher_span_test.go` (3), `mcp_handler_test.go` (3), `mcp_handler_unreachable_test.go` (2), `middleware/mcp_pipeline_integration_test.go` (1), `mcp_audit_regression_test.go` (3), `naming_test.go` (1); los 6 `TestComposer_CallTool_*` se reescriben sobre `Resolve`/`Invoke` (7 llamadas).
- [x] 8.4 `TestRPCGateway_ToolsCall_PreRequestBlock_SkipsUpstream` (`rpc_dispatcher_test.go:296`): añade `composer.EXPECT().Resolve(...)` y `AssertNotCalled("Invoke")`. Nuevos: `TestRPCGateway_ToolsCall_ResolveErrorSkipsPlugins` (tool desconocida y consentimiento pendiente sin `RunStage`), `TestRPCGateway_ToolsCall_SamePlanBothStages`. [§Bloqueo en PreRequest, §Errores de resolución antes de plugins, §Camino feliz]
- [x] 8.5 Gate: `go test ./pkg/app/mcp/... ./pkg/api/...`, `make test-race`, `make test-functional` (los 4 `TestMCPPluginChain_*` actuales sin cambios), `clean-comments`.

## Fase 9 — Commit 9 · slice 3c `tool-allowlist`

- [x] 9.1 `pkg/infra/plugins/toolallowlist/plugin.go`: `SupportedProtocols` → `{LLM, MCP}`; `Execute` bifurca en `in.Request.MCP`: tool de `Metadata[MetadataMCPTool]`; ausente → `okResult()`; `!keepTool` y `Blocks(mode)` → `newRejectResult(403, errToolDenied, [tool])` (→ `-32001` vía `blockToRPCError`, HTTP 200); observe → `SetDecision`; sin `on_empty_after_filter`. Rama LLM sin tocar. [mcp-tool-allowlist §Soporte ProtocolMCP, §Tool nativa, §Semántica, §Denegación en MCP]
- [x] 9.2 `plugin_test.go`: deny `*`, allow `run_*` (permite `run_query`, deniega `delete_table`), deny gana a allow, observe no bloquea, sin metadato → ok, federado (`Body.name` hash, metadata nativa). Tests LLM existentes intactos. `pkg/container/modules/plugins_test.go`: `tool_allowlist: {LLM, MCP}`. [§Plano LLM sin cambios]
- [x] 9.3 `tests/functional/mcp_plugin_chain_test.go` (+ `mcp_policy_scope_test.go`): `setupMCPPluginChainTwoUpstreams` (dos `startMCPUpstream`, dos registries en un consumer → nombres `mcp_<hash>_…`), `attachScopedPolicy(t, gw, consumer, payload, mcpScope)`; casos: por registry (solo su upstream; el stub TrustGuard cuenta), por tool, por grupo (token con `groups`), por usuario (`sub` y `email`), `except_groups`, `tool_allowlist deny *` → `-32001` con HTTP 200 y upstream a 0 llamadas, "solo Finanzas" (deny + except), sin scope = comportamiento actual, un `ListTools` por registry dentro del TTL en dos `tools/call` seguidas. [§Patrón "solo el grupo X"; mcp-policy-scope §Consumer federado; plan-selection §Camino feliz]
- [x] 9.4 `pkg/app/mcp/rpc_dispatcher_bench_test.go`: `BenchmarkRPCDispatcher_CallTool/{no_scope,static_scope,principal_scoped}` con composer stub y caché caliente, `-benchmem`. Cierra la Open Question del cap: añadir `maxPrincipalScopedPerDest` (32) solo si `principal_scoped` supera 2× `static_scope` en allocs.
- [x] 9.5 Gate: `go test -tags functional -race -count=1 -run MCPPluginChain ./tests/functional/` (local: solo compilación, `go vet -tags functional ./tests/functional/...`; la ejecución necesita la BD funcional), `go test -bench BenchmarkRPCDispatcher_CallTool -benchmem -run ^$ ./pkg/app/mcp/`, `clean-comments`.

## Fase 10 — Commit 10 · slice 3d `telemetry`

- [x] 10.1 `policy_plans.go`: `SkippedPolicy{PolicyRef; Reason}`, `ScopeDecision{Evaluated, Matched, Skipped}`; `Explain(reg, tool, principal) (*StagePlan, ScopeDecision)` desde `scoped` precompilado, mismo plan que `PlanFor`. `policy_plans_test.go`: "Tres policies con scope", "Excluida por excepción", "Sin policies con scope", `Explain ≡ PlanFor`. [mcp-policy-scope-telemetry §Estructura, §Cálculo solo con span activo]
- [x] 10.2 `pkg/infra/trace/span.go`: `MCPPolicyScope`, `MCPSkippedPolicy`, `MCPAttrs.PolicyScope`, `(*Span).SetMCPPolicyScope`.
- [x] 10.3 `rpc_dispatcher.go`: `trace.SpanFromContext(ctx) == nil` → `PlanFor`; con span → `Explain` + `SetMCPPolicyScope` antes de `PreRequest`, junto a `SetMCPUpstream`; nunca en discovery, meta-tools ni si `Resolve` falla. `rpc_dispatcher_span_test.go`: con span, sin span, tool desconocida sin decisión. [§Estampado en el span, §Alcance limitado a tools/call]
- [x] 10.4 `pkg/infra/metrics/events/event.go`: `MCP.PolicyScope *MCPPolicyScope json:"policy_scope,omitempty"`; `pkg/app/metrics/builder.go` `foldMCPSpans` lo copia; `PolicyEntry` sigue siendo solo ejecutados. `builder_test.go`: "Policy descartada" fuera de `policies[]`, "Doble TrustGuard (G3)" con dos entradas y la global en `Matched`, "Discovery" sin `policy_scope`. [§Emisión en el evento MCP]
- [x] 10.5 Gate: `go test ./pkg/app/mcp/... ./pkg/api/... ./pkg/app/metrics/... ./pkg/infra/trace/...`, `make test-race`, `clean-comments`.

## Fase 11 — Cierre (viaja en el commit 10)

- [x] 11.1 Pasada final `clean-comments` sobre todo `.go` tocado por la cadena (regla `~/.cursor/rules/go-comments.mdc`: doc comments exportados, directivas, "why" con ticket; nada narrativo).
- [x] 11.2 Crear `docs/mcp-policy-scope.md` (semántica `nil` vs `{}`, precedencia, patrón deny + `except_groups`, "Policies decide ejecución; toolkit y Access deciden visibilidad", `acts_for_users` sin grupos, despliegue data plane antes que control plane) y enlazarlo desde `docs/consumers-identity-model.md`.
- [x] 11.3 `design.md`: el enlace `docs/internal/mcp-granular-policies-exploration.md` apunta a una ruta gitignored (`.gitignore:57`); sustituir por `openspec/changes/mcp-granular-policies/exploration.md` y dejar nota de que `docs/internal/*` no se versiona.
- [x] 11.4 Marcar `[x]` aquí al fusionar cada PR; tras 3d, ejecutar `sdd-verify` contra los Success Criteria de `proposal.md`. — Evidencia (2026-09-17): `openspec/changes/mcp-granular-policies/verify-report.md` → PASS WITH WARNINGS (7 SC PASS, 2 PARTIAL por el escenario funcional en SKIP; 58/61 escenarios de spec COMPLIANT, 3 PARTIAL, 0 UNTESTED; funcional completa `-race` exit=0).

## Fuera de este cambio (seguimiento, no son tareas)

- `app`: UI de Policies Application → Registry → Tool, selector de grupos/usuarios; clave estable `DirectoryGroup.id` en `groupGrantKey.ts` / `resolveUserDirectoryGroups.ts`; revisar `MAX_BRIDGE_GROUPS` (G2).
- Admin API: `GET /v1/gateways/{gw}/registries/{id}/tools` para el selector (409 en registries per-principal); no bloquea.
- `multi-agent-tests`: `.env.dev` + `ENV_FILE` en su `Makefile` (G5), antes de 3c.
- Confirmar con `app` el contrato de attach (`204` vs `200 {"warnings"}`) antes de abrir 1d.
- Linear: sub-issues por PR bajo RUN-1597 si el equipo lo quiere (`sdd-linear-sync`).
