apiVersion: v1
kind: Service
metadata:
  name: agentgateway-proxy
  labels:
    app.kubernetes.io/instance: agentgateway
    app.kubernetes.io/component: proxy
    component: proxy
spec:
  type: ClusterIP
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/instance: agentgateway
    app.kubernetes.io/component: proxy
    component: proxy
