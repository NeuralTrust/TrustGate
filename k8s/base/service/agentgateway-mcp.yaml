apiVersion: v1
kind: Service
metadata:
  name: agentgateway-mcp
  labels:
    app.kubernetes.io/instance: agentgateway
    app.kubernetes.io/component: mcp
    component: mcp
spec:
  type: ClusterIP
  ports:
  - port: 8082
    targetPort: 8082
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/instance: agentgateway
    app.kubernetes.io/component: mcp
    component: mcp
